import * as TYPES from '../../../services/ActionTypes'

const initState = {
  pristupy: [],
  aktivita: []
}

export default (state = initState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
