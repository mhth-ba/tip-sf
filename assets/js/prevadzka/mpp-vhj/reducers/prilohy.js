import * as TYPES from '../../../services/ActionTypes'

const initState = {
  prilohy: [],

  loading: false,
  error: null
}

export default (state = initState, action) => {
  switch (action.type) {
    default:
      return state
  }
}